'use strict';

/**
 *  desk-cancellation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::desk-cancellation.desk-cancellation');
